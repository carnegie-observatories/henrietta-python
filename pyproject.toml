[project]
name = "henrietta"
version = "0.1.0"
description = "Henrietta Instrument Python Bindings"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=8.4.1",
]
